Combines channels in input into a single mono source, then plays that mono sound
to each channel at the volume given for that channel. The volume can be changed 
using [`set_volume`](ChannelVolume::set_volume).
