A source producing an infinite amount of Silence. Like all generators you
probably want to limit the duration of this source.

# Example
Padding a [`TakeDuration`](effects::TakeDuration) to guarantee an exact playtime:

```rust

```
